#!/bin/sh

set -e
us="`dirname "$0"`"
if test \! -e "$us"/lolwutconf/configure.inc; then
	git submodule update
fi
. "$us"/lolwutconf/configure.inc
conf_begin ${1+"$@"}
conf_mod app.build c efi
#
conf_efi_sdk_gnuefi
conf_end
